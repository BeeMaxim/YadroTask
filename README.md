# Компиляция на Lniux (проверено на g++)

```
mkdir build
cd build
cmake ..
make
./main ../test_gile.txt
```
# Компиляция на Windows

```
mkdir build
cd build
cmake ..
cmake --build .
Debug/main.exe ../test_file.txt
```
